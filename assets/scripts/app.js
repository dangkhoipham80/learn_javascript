const defaultResault = 0;
let currentResault = defaultResault;

currentResault = ((currentResault + 10) * 3) / 2 - 1;

let calcylationDescription = `( ${defaultResault} + 10 ) * 3 / 2 - 1`;
console.log(calcylationDescription);

let myOtherName;
const myName = "Khoi";
myName = "khoi";
alert(myName);
